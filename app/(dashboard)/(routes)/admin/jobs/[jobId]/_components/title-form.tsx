"use client"

interface TitleFormProps {
    initialData: {
        title: string,
    },
    jobId: string
}


const TitleForm = ({initialData, jobId} : TitleFormProps) => {
  return (
    <div>
      
    </div>
  )
}

export default TitleForm
